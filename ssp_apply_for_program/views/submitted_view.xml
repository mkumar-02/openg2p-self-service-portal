<odoo>
    <template id="your_template">
        <t t-if="show_toast">
            <script>
                odoo.define('your_module.your_template', function (require) {
                    var core = require('web.core');
                    var Dialog = require('web.Dialog');
                    var _t = core._t;

                    Dialog.alert(_t(toast_title), _t(toast_message), {
                        buttons: [{
                            text: _t("Close"),
                            classes: 'btn-primary',
                            close: true,
                        }]
                    });
                });
            </script>
        </t>
    </template>
</odoo>